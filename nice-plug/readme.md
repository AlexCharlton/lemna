# lemna-nice-plug

[nice-plug](https://codeberg.org/RustAudio/nice-plug) support for lemna, using the baseview backend.

```
cargo xtask bundle plugin-example-hello
```

Then plugins can be found in `./targe/bundled/`.
